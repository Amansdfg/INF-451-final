# 🚀 Quick Start Guide

## Быстрый старт за 3 шага

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 2: Обучение модели (опционально, но рекомендуется)

```bash
python models/train_model.py
```

Или обучите модель через веб-интерфейс на странице "ML Model".

### Шаг 3: Запуск приложения

```bash
streamlit run ui/app.py
```

Или используйте скрипт запуска:

```bash
python run.py
```

## Первое использование

1. **Откройте приложение** в браузере (обычно `http://localhost:8501`)

2. **В боковой панели:**
   - Введите тикер акции (например: `AAPL`, `TSLA`, `MSFT`)
   - Установите начальный баланс (по умолчанию $10,000)
   - Нажмите "🔄 Инициализировать систему"

3. **Обучите модель:**
   - Перейдите на страницу "ML Model"
   - Нажмите "🎓 Обучить модель"
   - Дождитесь завершения (может занять 1-2 минуты)

4. **Запустите симуляцию:**
   - Перейдите на страницу "Real-time Simulation"
   - Нажмите "▶️ Запустить цикл агентов"
   - Наблюдайте за работой агентов!

5. **Просмотрите результаты:**
   - Страница "Overview" - текущее состояние
   - Страница "Trade History" - история сделок и P&L

## Примеры тикеров для тестирования

- `AAPL` - Apple Inc.
- `TSLA` - Tesla Inc.
- `MSFT` - Microsoft Corporation
- `GOOGL` - Alphabet Inc.
- `AMZN` - Amazon.com Inc.

## Устранение проблем

### Ошибка "Model not found"
- Обучите модель через страницу "ML Model" или запустите `python models/train_model.py`

### Ошибка "No data available"
- Проверьте правильность тикера
- Убедитесь, что есть интернет-соединение
- Попробуйте другой тикер

### Ошибка импорта модулей
- Убедитесь, что вы находитесь в корневой директории проекта
- Проверьте, что все зависимости установлены: `pip install -r requirements.txt`

## Полезные команды

```bash
# Просмотр структуры проекта
tree -L 2

# Проверка установленных пакетов
pip list | grep -E "streamlit|yfinance|scikit-learn"

# Очистка данных (сброс истории)
rm data/history.csv

# Переобучение модели
rm models/model.pkl
python models/train_model.py
```

---

**Готово! Наслаждайтесь использованием системы! 🎉**

