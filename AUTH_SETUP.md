# Настройка JWT и SQLite для продакшена

## Что было добавлено

1. **JWT аутентификация** - система авторизации с токенами
2. **SQLite база данных** - хранение пользователей, портфелей и истории торгов
3. **Персональные портфели** - каждый пользователь видит только свой портфель

## Структура

```
project/
├── auth/
│   ├── __init__.py
│   ├── auth_manager.py      # JWT, регистрация, логин
│   └── middleware.py        # Проверка авторизации
├── database/
│   ├── __init__.py
│   └── db_manager.py        # Работа с SQLite
└── ...
```

## Установка зависимостей

```bash
pip install -r requirements.txt
```

Новые зависимости:
- `PyJWT>=2.8.0` - для JWT токенов
- `bcrypt>=4.0.0` - для хеширования паролей
- `python-dotenv>=1.0.0` - для переменных окружения

## Настройка для Streamlit Cloud

### 1. Секретный ключ JWT

В Streamlit Cloud:
1. Перейдите в настройки приложения
2. Откройте "Secrets"
3. Добавьте:

```toml
JWT_SECRET_KEY = "your-super-secret-random-string-here"
```

Или создайте файл `.streamlit/secrets.toml` локально (не коммитьте его!):

```toml
JWT_SECRET_KEY = "your-super-secret-random-string-here"
```

### 2. База данных SQLite

SQLite файл будет автоматически создан в `data/trading_system.db`.

**Важно для Streamlit Cloud:**
- Файл БД будет сохраняться между сессиями
- Каждый пользователь будет иметь свой портфель
- Данные будут изолированы по `user_id`

## Использование

### Регистрация нового пользователя

1. Откройте приложение
2. Перейдите на вкладку "Регистрация"
3. Введите:
   - Имя пользователя
   - Email
   - Пароль (минимум 6 символов)
   - Подтверждение пароля

### Вход в систему

1. Введите имя пользователя и пароль
2. Нажмите "Войти"
3. JWT токен будет сохранен в session state

### Выход из системы

Нажмите кнопку "🚪 Выйти" в боковой панели

## База данных

### Таблицы

1. **users** - пользователи системы
   - id, username, email, password_hash, created_at

2. **portfolios** - портфели пользователей
   - id, user_id, balance, created_at, updated_at

3. **holdings** - холдинги (акции в портфеле)
   - id, user_id, ticker, shares, avg_price, total_cost

4. **trade_history** - история торгов
   - id, user_id, timestamp, ticker, action, shares, price, total, balance_after, confidence

### Изоляция данных

Все запросы к БД фильтруются по `user_id`, поэтому:
- Каждый пользователь видит только свой портфель
- История торгов изолирована
- Холдинги привязаны к пользователю

## Безопасность

1. **Пароли** хешируются с помощью bcrypt
2. **JWT токены** имеют срок действия (7 дней)
3. **Секретный ключ** должен быть уникальным и сложным
4. **HTTPS** используется автоматически на Streamlit Cloud

## Миграция существующих данных

Если у вас уже есть данные в CSV файлах:
- Старые данные останутся в `data/history.csv`
- Новые данные будут сохраняться в БД
- Можно написать скрипт миграции для переноса данных

## Отладка

Если что-то не работает:

1. Проверьте, что все зависимости установлены
2. Убедитесь, что JWT_SECRET_KEY установлен
3. Проверьте логи в Streamlit Cloud
4. Убедитесь, что файл БД создается в правильной директории

## Обратная совместимость

Система поддерживает работу без авторизации (для тестирования):
- Если `user_id` не указан, используется CSV файлы
- Установите `use_db=False` в `ExecutionAgent` для старого режима

