# Быстрый старт с JWT и SQLite

## ✅ Что было реализовано

1. ✅ JWT аутентификация с токенами
2. ✅ SQLite база данных для всех пользователей
3. ✅ Персональные портфели для каждого пользователя
4. ✅ Регистрация и вход в систему
5. ✅ Работает на Streamlit Cloud

## 🚀 Запуск

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Настройте секретный ключ (для продакшена)

**Для Streamlit Cloud:**
1. Откройте настройки приложения в Streamlit Cloud
2. Перейдите в "Secrets"
3. Добавьте:
```toml
JWT_SECRET_KEY = "ваш-секретный-ключ-здесь"
```

**Для локального запуска:**
Создайте файл `.streamlit/secrets.toml`:
```toml
JWT_SECRET_KEY = "ваш-секретный-ключ-здесь"
```

### 3. Запустите приложение

```bash
streamlit run ui/app.py
```

## 📝 Использование

### Первый запуск

1. Откройте приложение в браузере
2. Вы увидите страницу входа
3. Перейдите на вкладку "Регистрация"
4. Создайте аккаунт:
   - Имя пользователя
   - Email
   - Пароль (минимум 6 символов)
5. После регистрации вы автоматически войдете в систему

### Вход в систему

1. Введите имя пользователя и пароль
2. Нажмите "Войти"
3. Вы увидите свой персональный портфель

### Работа с системой

- Каждый пользователь имеет свой портфель
- Баланс сохраняется в БД
- История торгов привязана к пользователю
- Все данные изолированы

## 🗄️ База данных

База данных SQLite создается автоматически в `data/trading_system.db`

**Таблицы:**
- `users` - пользователи
- `portfolios` - портфели
- `holdings` - холдинги (акции)
- `trade_history` - история торгов

## 🔒 Безопасность

- Пароли хешируются с помощью bcrypt
- JWT токены действительны 7 дней
- Каждый пользователь видит только свои данные
- Все запросы фильтруются по `user_id`

## 📊 Особенности

1. **Персональные портфели** - каждый пользователь имеет свой баланс и холдинги
2. **История торгов** - привязана к пользователю
3. **Автоматическое создание портфеля** - при регистрации создается портфель с балансом $10,000
4. **Работает на Streamlit Cloud** - SQLite файл сохраняется между сессиями

## 🐛 Решение проблем

### Ошибка "Model not found"
Обучите модель на странице "ML Model" перед использованием

### Ошибка авторизации
- Проверьте, что JWT_SECRET_KEY установлен
- Попробуйте выйти и войти снова

### База данных не создается
- Убедитесь, что директория `data/` существует
- Проверьте права доступа к файловой системе

## 📚 Дополнительная информация

См. `AUTH_SETUP.md` для подробной документации

